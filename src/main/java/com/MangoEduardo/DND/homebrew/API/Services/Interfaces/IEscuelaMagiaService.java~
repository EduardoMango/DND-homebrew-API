package com.MangoEduardo.DND.homebrew.API.Services.Interfaces;

import com.MangoEduardo.DND.homebrew.API.Domain.DTO.Resources.EscuelaMagiaDTO;
import com.MangoEduardo.DND.homebrew.API.Domain.Entities.Resources.EscuelaMagiaEntity;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;

import java.util.Optional;


public interface IEscuelaMagiaService {

    Page<EscuelaMagiaDTO> findAll(Pageable pageable);

    Optional<EscuelaMagiaDTO> findById(Long id);

    EscuelaMagiaDTO save(EscuelaMagiaDTO escuelaMagiaDTO);

    boolean isExist(Long id);

    EscuelaMagiaDTO update(Long id, EscuelaMagiaDTO escuelaMagiaDTO);
    void delete(Long id);

    Page<EscuelaMagiaDTO> findByNombreEscuela(String nombreEscuela, Pageable pageable);
}
